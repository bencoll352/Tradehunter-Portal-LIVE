# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

# The service account that the App Hosting backend will use to run and
# access other Google Cloud services. The service account must have the
# "Firebase Admin" role.
# For more details, see:
# https://firebase.google.com/docs/app-hosting/configure#service-account
serviceAccount: tradehunter-pro-portal@tradehunter-pro-portal.iam.gserviceaccount.com

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
